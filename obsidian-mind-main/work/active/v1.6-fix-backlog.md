---
date: 2026-07-06
description: "Consolidated, priority-ordered fix backlog from the 2026-07-06 v1.6 whole-project audit — 16 items from curated-level closure down to dev-tool relocation."
tags:
  - work-note
quarter: Q3-2026
status: active
---

# v1.6 Fix Backlog (from the 2026-07-06 audit)

Consolidated fix list produced by the v1.6 whole-project audit, in priority order. Items are checked off as they land; the closure-pass items (1, 5) are being executed first as the prerequisite for the [[work/active/v1.6-level-editor-refactor|editor split]].

1. [ ] **Curated level + tail closure** — `levels/v1.json`, closes both v1.5 tails (see pre-v1.6 closure pass in [[brain/Key Decisions]])
2. [ ] **Editor F5 leak** — live TriggerZone/AmmoPickup + parked bullets survive play-mode exit (see [[brain/Gotchas]])
3. [ ] **`start_game()` duplicate teardown** — `main.py:401-409` should route through `game.return_to_menu()` (see [[brain/Gotchas]])
4. [ ] **`compat.py` shader-patch extraction** — `_patch_shaders_to_glsl120()` copy lives in both `main.py` and `level_editor.py`
5. [ ] **Checkpoint consumer** — `game.respawn_point` store-only; bundled into the closure pass
6. [ ] **`window.color` clamp** — `level_editor.py:4118`, unguarded (see [[brain/Gotchas]])
7. [ ] **CLAUDE.md drift batch** — stale line numbers and closed-item rows in the tech-debt table
8. [ ] **Dead `window.on_resize` recenter** — remove the dead assignment (see [[brain/Gotchas]] `window.on_resize` entry)
9. [ ] **`weapon.py` viewmodel comment contradiction** — comment text disagrees with the shipped dual-camera behaviour
10. [ ] **Gizmo pick unguarded `e.name` sweep** — missing `_is_live()` guard (Hard Constraint 13 family)
11. [ ] **`save_level()` unguarded write** — same class as the `_save_prefs()` tech-debt row
12. [ ] **Eternal debug collider lines** — `player_controller.py:76`, 12 eternal entities survive menu transitions
13. [ ] **Confirm/document air-shoot intent** — shoot not gated on `grounded`; intentional or regression?
14. [ ] **Bare-string texture load path** — route the four bare sites through `_resolve_texture` (see [[brain/Gotchas]] bare-string entry)
15. [ ] **Relocate `_resolve_model`/`_resolve_texture` out of `undo_redo.py`** — wrong module for asset resolution
16. [ ] **Move dev tools to `tools/`** — `auto_fix.sh`, `auto_fix_loop.py`, `v1_3_step_runner.py` out of `Scripts/`

## Related

- [[work/active/v1.6-level-editor-refactor]] — the milestone this backlog feeds
- [[brain/Gotchas]] — root-cause entries for items 2, 3, 6, 8, 14
- [[brain/Key Decisions]] — closure-pass sequencing decision
- [[brain/North Star]] — audit summary + current focus
