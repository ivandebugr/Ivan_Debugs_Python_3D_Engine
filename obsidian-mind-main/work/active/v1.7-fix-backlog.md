---
date: 2026-07-07
description: "Open items from the v1.7 cycle — editor interaction fixes shipped, four loose ends still open: ambient track, C4/C5 decision, and two stray unstaged working-tree changes."
tags:
  - work-note
quarter: Q3-2026
status: active
---

# v1.7 Fix Backlog

Mirrors [[work/archive/2026/v1.6-fix-backlog]]'s structure. v1.7's editor-interaction
work (drag-lock, convert-entity-type, inspector layout, smoke harness) is DONE and
committed — see commits `f07a37c`, `b29e79b`, `52857e6`, `466f2c3`. This doc tracks what's
still genuinely open, so it doesn't depend on chat memory to survive.

## Open

1. [ ] **Ambient/music track** — unmet since the v1.3 remainder (itch.io ship prep). CC-0
   track, no source pulled yet. One-shot SFX half of this closed 2026-07-07
   (`blaster.ogg`/`blaster_repeater.ogg` wired on weapon fire); the ambient/music half is
   the last open item in that remainder. See [[brain/North Star]] "v1.3 remainder" line,
   CHANGELOG line noting "no ambient/music track is wired."
2. [ ] **C4/C5 layout decision (deferred)** — movable-but-fixed-dock panels (C4) and true
   Unity-style drag-to-dock (C5) were explicitly parked in
   [[v1.7-editor-ux-scoping]] pending how the now-shipped C1 (collapsible panels)/C3
   (resizable panels) feel in practice. B1, B2-field, B2-ring, C1, C2, C3 have all
   shipped (commits `c146be9`, `114d815`, `05dcf3f`, `6983d82`, `e1c4ce6`, `bd556f0`).
   Decision needed: does the editor's UX still want framework-scale investment (C4:
   ~2-3 days, C5: 2-4+ weeks and not fully feasible on Ursina/Panda3D — see the scoping
   doc's feasibility verdict), or is C1-C3 "enough" for a solo editor? No code work
   until Ivan calls it.
3. [ ] **Stray `level.json` reformat** — working tree has a large (967+/48-) diff that's
   pure JSON re-formatting (compact array → one-key-per-line), not new level content.
   Almost certainly an artifact of opening+saving the level in the editor during this
   session's/a prior session's verification passes. Not committed — needs a decision:
   revert to the compact form (`git checkout -- level.json`) or accept the reformat if
   the new pretty-printed style is preferred going forward.
4. [ ] **`obsidian-mind-main/work/active/v1.7-editor-ux-scoping.md` un-archive** — the
   working tree currently has this file both deleted from `work/archive/2026/` and
   recreated (untracked) in `work/active/`, with its own `status: completed` frontmatter
   unchanged. Looks like a prior session moved it back to active without finishing the
   status/frontmatter update for the *new* round of editor work (this session's B-lock/
   convert/layout steps) that isn't actually documented in that scoping doc. Needs a
   decision: either fold this session's work into that doc as a new dated addendum and
   move it back to archive, or leave it in active with a corrected status if more
   scoping-relevant work is expected here.

## Closed this cycle (not previously tracked in a doc)

- [x] **Editor drag interactions** — DONE 2026-07-07, `f07a37c` — active-drag-owner lock
  (`_drag_owner`) so gizmo/splitter/browser drags can't re-target mid-drag; gizmo
  translate/rotate mode separation so tips and rings are never simultaneously pickable.
- [x] **Trigger/pickup type conversion** — DONE 2026-07-07, `b29e79b` — inspector "Convert
  to" row + `ConvertEntityCommand`; previously the only way to create a trigger/pickup
  was dragging a fresh card from the browser, with no way to convert an already-placed
  block.
- [x] **Inspector layout cleanup** — DONE 2026-07-07, `52857e6` — Transform header +
  tightened grid pitch, Texture/Model rows now hide (not blank) for non-block
  selections, readable trigger action labels + legend.
- [x] **`win_then_r`/`gameover_then_r` smoke scenarios** — DONE 2026-07-07, `466f2c3` —
  harness now dismisses the v1.7 `IntroScreen` splash before searching for the
  main-menu Play button. All 4 standing smoke scenarios (`load_and_close`, `win_then_r`,
  `gameover_then_r`, `editor_f5_roundtrip`) verified green.
- [x] **C1 collapse-cascade "ghost interaction" — investigated, does NOT reproduce** —
  a prior session's [[brain/Gotchas]] entry claimed collapsing the inspector panel left
  sub-widgets clickable behind the collapsed space. Re-verified 2026-07-07 both
  statically (Panda3D's `CollisionTraverser` skips stashed subtrees; Ursina's `input()`
  dispatch gates on `has_disabled_ancestor()`) and empirically (a live `mouse.update()`
  against a button behind a disabled parent returns `hovered_entity is None`). The
  original Gotchas entry was corrected in place rather than deleted, so this doesn't
  get "rediscovered" as a live bug. No code change needed.

## Related

- [[v1.7-editor-ux-scoping]] — the scoping report this cycle's B/C work executed against
- [[work/archive/2026/v1.7-editor-ux-bundle]] — the first shipped slice (B1/B2-field/C1/C2)
- [[brain/Gotchas]] — active-drag-owner-lock pattern, inspector layout root causes, the
  corrected C1 collapse entry, the corrected smoke-harness entry
- [[work/archive/2026/v1.6-fix-backlog]] — prior cycle's backlog, same structure
- [[brain/North Star]] — v1.3 remainder (ambient track) tracking
